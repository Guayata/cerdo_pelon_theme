<div id="pagination" class="clearfix">
    <ul class="pagination pull-right justify-content-center m-0 py-4">
        {% if paginate.previous %}
            <li class="pagination_previous">
                <a href="{{ paginate.previous.url }}" title="{{ paginate.previous.title }}">
                   {% include 'icon-chevron-left' %}
                </a>
            </li>
        {% else %}
            <li class="pagination_previous disabled hide">
                <span>
                    {% include 'icon-chevron-left' %}
                </span>
            </li>
        {% endif %}
        {% for part in paginate.parts %}
            {% if part.is_link %}
                <li>
                    <a href="{{ part.url }}" title="">{{ part.title }}</a>
                </li>
            {% else %}
                {% if part.title == paginate.current_page %}
                    <li class="active"><a  title="">{{ part.title }}</a></li>
                {% else %}
                    <li><span>{{ part.title }}</span></li>
                {% endif %}
            {% endif %}
        {% endfor %}
        {% if paginate.next %}
            <li class="pagination_next">
                <a href="{{ paginate.next.url }}" title="{{ paginate.next.title }}">
                {% include 'icon-chevron-right' %}
                </a>
            </li>
        {% else %}
            <li class="pagination_next disabled hide">
                <span>
                {% include 'icon-chevron-right' %}
                </span>
            </li>
        {% endif %}
    </ul>
</div>
